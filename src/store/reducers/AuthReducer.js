const initialState = {
    name: 'John Doe',
    age: 30,
    email: 'johndoe@example.com'
};

const authReducer = (state = initialState, action) => {
    return state;
};

export default authReducer;